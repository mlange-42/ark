version: "2"

run:
  timeout: 5m
  tests: true
linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
    - errcheck
    - revive
    - unused
    - gocyclo
    - misspell
    - copyloopvar
    - gocheckcompilerdirectives
    - gocritic
    - godoclint
    - intrange
    - testableexamples
    - unconvert
  settings:
    revive:
      rules:
        - name: unused-parameter
          exclude: ["TEST"]
    gocritic:
      disabled-checks:
        - commentFormatting

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
